{
  "name": "calendar-monorepo",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "lint": "npm run lint --prefix react",
    "build": "npm run build --prefix react",
    "dev": "npm run dev --prefix react",
    "test": "npm test --prefix react"
  }
}

